# AutoCare Dashboard Environment Configuration
# Copy this file to .env.local and modify values as needed

# Backend API URL - Change only if backend runs on different port/host
VITE_API_BASE_URL=http://localhost:3000/api

# Default settings (no changes usually needed)
VITE_APP_NAME=AutoCare Dashboard
